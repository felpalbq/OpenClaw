# Credenciais e secrets
.env

# Banco de dados SQLite de estado (volatil)
state.db
state.db-shm
state.db-wal

# Sessoes e logs (volateis)
sessions/
logs/

# Caches (regeneraveis)
cache/
audio_cache/
image_cache/

# Binarios instalados (regeneraveis via npm install)
bin/

# Diretorios de runtime volateis
pairing/
sandboxes/

# Arquivos de lock e PID
gateway.lock
gateway.pid
auth.lock

# Estado volatil do gateway
gateway_state.json

# Caches gerados automaticamente
context_length_cache.yaml
.update_check
.skills_prompt_snapshot.json
